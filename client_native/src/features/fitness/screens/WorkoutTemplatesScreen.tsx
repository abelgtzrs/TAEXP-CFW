import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export function WorkoutTemplatesScreen() {
  return (
    <PlaceholderScreen
      icon="W"
      title="Workout Templates"
      subtitle="Build and manage reusable workout templates for faster session planning."
    />
  );
}
