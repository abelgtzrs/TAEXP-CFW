import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export function WorkoutEditorScreen() {
  return (
    <PlaceholderScreen
      icon="S"
      title="Workout Session"
      subtitle="Log sets, reps, and notes during a workout with real-time session progress."
    />
  );
}
