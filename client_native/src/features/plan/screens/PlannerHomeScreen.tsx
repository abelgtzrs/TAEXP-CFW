import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export function PlannerHomeScreen() {
  return (
    <PlaceholderScreen
      icon="📋"
      title="Planner"
      subtitle="Tasks and habits in one view. Toggle between them with a segmented switch."
    />
  );
}
