import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export function BookNotesScreen() {
  return (
    <PlaceholderScreen
      icon="N"
      title="Book Notes"
      subtitle="Capture and organize highlights, takeaways, and action notes per book."
    />
  );
}
