import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export function BookDetailsScreen() {
  return (
    <PlaceholderScreen
      icon="B"
      title="Book Details"
      subtitle="Review reading progress, metadata, and reading targets for a selected book."
    />
  );
}
